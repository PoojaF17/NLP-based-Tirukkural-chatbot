# Copy this to .env and put it in the project root (same folder as requirements.txt)
# If you have an OpenAI API key, paste it below to enable LLM responses.
OPENAI_API_KEY=sk-REPLACE_ME
# Optional: choose an OpenAI chat model
OPENAI_MODEL=gpt-4o-mini
